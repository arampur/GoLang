package main

import "fmt"

func main() {
	fmt.Println(identify_adjacent("abcd", 2))
}

func identify_adjacent(s string, k int) string {
	res := ""

	// iterate string and store in map with char counter

	// iterate map and do the modulus of the char count by k to get the value of count
	// we need in the final string

	//iterate and store the char with count in the res and lastly return res
	return res
}
